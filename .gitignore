# Dependencies
node_modules/

# Build output
dist/
build/
.vite/
*.tsbuildinfo

# Environment — never commit credentials
.env
.env.*
!.env.example
*.local

# Test and tooling output
coverage/
playwright-report/
test-results/
.playwright/
npm-debug.log*
*.log

# Local scratch files (bug.tmp.ts, edge.tmp.ts and friends are bug-hunt probes,
# not product code — they must never enter the repo or the lint gate)
*.tmp.ts
*.tmp.tsx
*.tmp.js

# Local data
.data/

# OS
.DS_Store
Thumbs.db
desktop.ini
